version: '3'
services:  
  sholat-yuk:
    container_name: sholat-yuk
    restart: unless-stopped
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - '3131:3131'
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/node_modules
